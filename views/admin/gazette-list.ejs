<%
  locals.activePage = 'gazette';
  locals.flash_success = flash_success;
  locals.flash_error = flash_error;
%>
<%- include('partials/admin-layout-top') %>

<div class="page-header d-flex justify-content-between align-items-center mb-4">
  <h2 class="page-title"><i class="fas fa-newspaper me-2"></i>Газета «Ветеран»</h2>
</div>

<!-- Downloadable file -->
<div class="admin-card mb-4">
  <div class="admin-card-header"><h5 class="mb-0"><i class="fas fa-file-pdf me-2"></i>Файл для скачивания</h5></div>
  <div class="admin-card-body">
    <% if (gazetteFile) { %>
    <div class="mb-3">
      <a href="<%= gazetteFile %>" target="_blank" class="btn btn-outline-secondary btn-sm">
        <i class="fas fa-eye me-1"></i>Текущий файл
      </a>
    </div>
    <% } %>
    <form action="/admin/gazette/file" method="POST" enctype="multipart/form-data" class="row g-3 align-items-end">
      <div class="col-md-8">
        <label class="form-label fw-medium">Загрузить файл номера (PDF)</label>
        <input type="file" name="file" class="form-control" required>
      </div>
      <div class="col-md-4">
        <button type="submit" class="btn btn-danger w-100">
          <i class="fas fa-upload me-1"></i>Загрузить
        </button>
      </div>
    </form>
    <% if (gazetteFile) { %>
    <form action="/admin/gazette/file/delete" method="POST" class="mt-3"
          onsubmit="return confirm('Удалить файл?')">
      <button type="submit" class="btn btn-outline-danger btn-sm">
        <i class="fas fa-trash me-1"></i>Удалить файл
      </button>
    </form>
    <% } %>
  </div>
</div>

<!-- Upload photo -->
<div class="admin-card mb-4">
  <div class="admin-card-header"><h5 class="mb-0"><i class="fas fa-upload me-2"></i>Добавить страницу газеты</h5></div>
  <div class="admin-card-body">
    <form action="/admin/gazette/photos/new" method="POST" enctype="multipart/form-data" class="row g-3 align-items-end">
      <div class="col-md-8">
        <label class="form-label fw-medium">Фото страницы (верстка)</label>
        <input type="file" name="image" class="form-control" accept="image/*" required>
      </div>
      <div class="col-md-4">
        <button type="submit" class="btn btn-danger w-100">
          <i class="fas fa-upload me-1"></i>Загрузить
        </button>
      </div>
    </form>
  </div>
</div>

<!-- Photo grid -->
<div class="admin-card">
  <div class="admin-card-body">
    <% if (photos.length > 0) { %>
    <div class="row g-3">
      <% photos.forEach(p => { %>
      <div class="col-md-3 col-sm-4 col-6">
        <div class="position-relative">
          <img src="<%= p.image %>" alt="" class="img-fluid rounded" style="width:100%;height:160px;object-fit:cover">
          <form action="/admin/gazette/photos/<%= p.id %>/delete" method="POST"
                onsubmit="return confirm('Удалить страницу?')"
                class="position-absolute top-0 end-0 m-1">
            <button type="submit" class="btn btn-xs btn-outline-danger bg-white">
              <i class="fas fa-trash"></i>
            </button>
          </form>
        </div>
      </div>
      <% }) %>
    </div>
    <% } else { %>
    <div class="text-center p-5 text-muted">
      <i class="fas fa-images fa-3x mb-3"></i>
      <p>Страниц пока нет</p>
    </div>
    <% } %>
  </div>
</div>

<%- include('partials/admin-layout-bottom') %>
